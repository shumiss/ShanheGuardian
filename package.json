{
  "name": "qianzhi-public-data-platform",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "backend": "npm --prefix backend start",
    "backend:test": "npm --prefix backend test",
    "frontend:dev": "npm --prefix frontend run dev",
    "frontend:build": "npm --prefix frontend run build"
  }
}

